# Pinned to the versions used to produce the manuscript results (May 2026).
# Newer minor releases of these packages should also work; major version
# bumps may require code changes.

numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
scikit-learn>=1.4,<2
matplotlib>=3.8,<4
openpyxl>=3.1,<4

# TensorFlow is required for the ANN calibrator only.
# If you do not need the ANN, you can comment this out -- the other four
# calibrators (Affine, MLR, RF, GBR) work without it.
tensorflow>=2.16,<3
